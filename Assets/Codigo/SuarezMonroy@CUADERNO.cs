using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//Nuestro objeto jugadores una esfera, sin material.
//El componente Transformar determina la posición , la rotación y la escala de cada objeto en la escena.
//Los valores de posición, rotación y escala de una Transformación se miden en relación con el padre de la Transformación. Si la transformación no tiene padre, las propiedades se miden en el espacio mundial.
//Si desactivamos el script y colocamos la camara como hija del jugador, sucede un fallo que no nos permite ni visualizar la escena.

